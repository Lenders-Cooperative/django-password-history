============
Installation
============

At the command line::

    $ easy_install django-password-history

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-password-history
    $ pip install django-password-history
